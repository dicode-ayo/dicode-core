name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# Least-privilege GITHUB_TOKEN default for every job. A job needing more (see
# comment-pr) overrides this wholesale — a job-level block replaces, not
# extends, the workflow-level one.
permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      # Deno is required for pkg/runtime/deno integration tests (e.g.
      # TestRun_SecretOutputRoutedToChannel). Without it those tests t.Skip
      # silently and a regression in the runtime IPC path goes uncaught.
      # See issue #237.
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      # Python is required for the dicode_sdk.py unit tests
      # (TestPythonSDK in pkg/runtime/python). Without it the wrapper
      # t.Skip's silently and SDK regressions go uncaught. See issue #39.
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Run tests
        run: go test -timeout 300s -race ./...

  test-tasks:
    name: Test tasks (Deno)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      # Only tasks/examples/** entries that ship a task.test.ts run here — see
      # issue #551. Today that's deploy-wizard, repo-prune, and suspend-wizard,
      # all runtime: deno. The Layer-2 unit-test harness only covers Deno
      # (tasks/sdk-test.ts) and Python (tasks/sdk_test.py, #159 Phase 2) — a
      # docker/podman example (hello-docker, hello-podman, nginx-start) can't
      # ship a test file yet (#159 Phase 3 remains unstarted). hello-python's
      # task.test.py is run separately below (it's a PEP 723 script executed
      # via `uv run`, not `deno test`, so this glob correctly skips it).
      - name: Run Deno tests for tasks/examples/*
        run: deno test --allow-all --config=tasks/deno.json 'tasks/examples/**/task.test.ts'

      # repo-prune's task.test.ts mocks Deno.Command, so it never exercises
      # prune-stale-refs.sh itself — the script that does the actual
      # branch/worktree deletion. This shell-level suite drives the real
      # script against a throwaway repo (gh stubbed on PATH, no network).
      - name: Run repo-prune shell guard tests
        run: bash tasks/examples/repo-prune/prune-stale-refs.test.sh

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          version: "0.12.7"

      # Python's Layer-2 harness (#159 Phase 2): tasks/examples/hello-python
      # ships the one Python task.test.py in this repo today. Each
      # task.test.py is a self-running PEP 723 script (see
      # docs/concepts/testing.md § Python for the exact contract — the same
      # one pkg/tasktest.runPython relies on), so a plain `uv run <file>`
      # exercises it directly; no daemon needed.
      - name: Run Python task test for tasks/examples/hello-python
        run: uv run tasks/examples/hello-python/task.test.py

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: go vet
        run: go vet ./...

      - name: Check go.mod and go.sum are tidy
        run: |
          if ! go mod tidy -diff; then
            echo "go.mod/go.sum are not tidy. Run 'go mod tidy' and commit the result."
            exit 1
          fi

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "The following files need formatting:"
            echo "$unformatted"
            exit 1
          fi

  build-pr:
    name: Build PR artifacts
    if: github.event_name == 'pull_request'
    needs: [test, lint]
    runs-on: ubuntu-latest
    outputs:
      url-linux-amd64:   ${{ steps.upload-linux-amd64.outputs.artifact-url }}
      url-linux-arm64:   ${{ steps.upload-linux-arm64.outputs.artifact-url }}
      url-darwin-amd64:  ${{ steps.upload-darwin-amd64.outputs.artifact-url }}
      url-darwin-arm64:  ${{ steps.upload-darwin-arm64.outputs.artifact-url }}
      url-windows-amd64: ${{ steps.upload-windows-amd64.outputs.artifact-url }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - uses: goreleaser/goreleaser-action@v7
        with:
          version: "~> v2"
          args: build --clean --snapshot
        env:
          GORELEASER_CURRENT_TAG: pr-${{ github.event.pull_request.number }}

      - name: List dist
        run: find dist -type f | sort

      - name: Upload linux/amd64
        id: upload-linux-amd64
        uses: actions/upload-artifact@v7
        with:
          name: dicode-linux-amd64
          path: dist/dicode_linux_amd64_v1/dicode
          retention-days: 7

      - name: Upload linux/arm64
        id: upload-linux-arm64
        uses: actions/upload-artifact@v7
        with:
          name: dicode-linux-arm64
          path: dist/dicode_linux_arm64/dicode
          retention-days: 7

      - name: Upload darwin/amd64
        id: upload-darwin-amd64
        uses: actions/upload-artifact@v7
        with:
          name: dicode-darwin-amd64
          path: dist/dicode_darwin_amd64_v1/dicode
          retention-days: 7

      - name: Upload darwin/arm64
        id: upload-darwin-arm64
        uses: actions/upload-artifact@v7
        with:
          name: dicode-darwin-arm64
          path: dist/dicode_darwin_arm64/dicode
          retention-days: 7

      - name: Upload windows/amd64
        id: upload-windows-amd64
        uses: actions/upload-artifact@v7
        with:
          name: dicode-windows-amd64
          path: dist/dicode_windows_amd64_v1/dicode.exe
          retention-days: 7

  comment-pr:
    name: Comment artifacts on PR
    if: github.event_name == 'pull_request'
    needs: build-pr
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Post sticky comment
        uses: marocchino/sticky-pull-request-comment@v3
        with:
          header: build-artifacts
          message: |
            ## Build Artifacts

            Dev binaries for `${{ github.event.pull_request.head.sha }}`:

            | Platform | Download |
            | --- | --- |
            | linux/amd64 | [dicode-linux-amd64](${{ needs.build-pr.outputs.url-linux-amd64 }}) |
            | linux/arm64 | [dicode-linux-arm64](${{ needs.build-pr.outputs.url-linux-arm64 }}) |
            | darwin/amd64 | [dicode-darwin-amd64](${{ needs.build-pr.outputs.url-darwin-amd64 }}) |
            | darwin/arm64 (Apple Silicon) | [dicode-darwin-arm64](${{ needs.build-pr.outputs.url-darwin-arm64 }}) |
            | windows/amd64 | [dicode-windows-amd64.exe](${{ needs.build-pr.outputs.url-windows-amd64 }}) |

            > Artifacts expire after 7 days.
